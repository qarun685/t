# Task Manager

Simple Laravel project for managing tasks. Project is in early stages, functionality is minimal.

## Installation

Clone repo, install dependencies, configure env, migrate DB:

```bash
git clone https://github.com/qarun685/task-manager.git
cd task-manager
composer install
php artisan migrate
